const user=localStorage.getItem('skillbridge_user')||'demo-student';
const role=()=>localStorage.getItem('skillbridge_role')||'student';
async function api(url,opt={}){const h=new Headers(opt.headers||{});h.set('X-Local-User',user);h.set('X-Local-Role',role());if(opt.body&&!h.has('Content-Type'))h.set('Content-Type','application/json');const r=await fetch(url,{...opt,headers:h});const d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error||'Request failed');return d;}
async function inject(){if(role()!=='student'||document.getElementById('candidate-optin'))return;const box=document.createElement('div');box.id='candidate-optin';box.className='candidate-optin';box.innerHTML='<div><b>Recruiter discovery</b><p>Opt in to let verified organizations discover your profile after filtering the aggregate talent pool.</p></div><label><input id="candidate-toggle" type="checkbox"> Available</label>';document.body.appendChild(box);try{const s=await api('/api/profile/candidate-opt-in');document.getElementById('candidate-toggle').checked=Boolean(s.candidateOptIn);}catch(e){box.remove();return;}document.getElementById('candidate-toggle').onchange=async e=>{try{await api('/api/profile/candidate-opt-in',{method:'PUT',body:JSON.stringify({candidateOptIn:e.target.checked})});}catch{e.target.checked=!e.target.checked;}};}
setInterval(inject,900);inject();
